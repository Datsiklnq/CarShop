@model IEnumerable<CarShop.ModelViews.CarCustomer>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Car.Brand)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Customer.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayNameFor(model => model.Car.Brand)
            </td>
            <td>
                @Html.DisplayNameFor(model => model.Customer.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Purchase.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Purchase.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Purchase.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
